//Simple Hello World on Flutter Web
1. Install the Fullter SDK and configure the PATH whereever you have Flutter.bat
2. Install the VSCode extension for the Flutter
3.enable flutter using below commands

/**
 flutter channel beta
 flutter upgrade
 flutter config --enable-web
 --yes
 **/
